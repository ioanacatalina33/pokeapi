const size = {
  sm: "576px",
  md: "768px",
  lg: "1200px",
};

const device = {
  sm: `(min-width: ${size.sm})`,
  md: `(min-width: ${size.md})`,
  lg: `(min-width: ${size.lg})`,
};

const colors = {
  colorText: "#444444",
  background: "rgba(255,255,255,0.8)",
  primary: "#fdcd00",
  primaryLight: "#fcdd20",
  secondary: "#bd1b08",
  pokemonShadow: "#3555a8",
};

const sizes = {
  fontSize: "1.2rem",
};

export {size, device, colors, sizes};
